# ⚙ 入出金・売上帳簿 自動更新 設定

- メールソース：`tosinariy@gmail.com`（Gmail コネクタ接続済み）に、`ecbf.info@gmail.com` からの**自動転送**で届く金融機関・決済通知
  - 転送は 2026-08-09 に開通（承認＋「受信メールを転送する」ON 済み）
- 帳簿マスタ（本リポジトリ）：`company/secretary/経理/売上帳簿/入出金・売上帳簿_2026.xlsx`（＋ `.md` 要約）
- Drive保存先：`入出金・売上帳簿（電子CB農法）` フォルダ
  - folderId: `1tEo7xWuaZiWZZKFsHQ7XNwUuVovm8BkZ`
- 新規判定の基準（この日時より後に受信したメール＝新規として処理）：
  - LAST_CHECK: 2026-08-11T22:41:16Z

## 対象メールの見つけ方（Gmail 検索クエリ）
- 主クエリ：`to:ecbf.info@gmail.com newer_than:2d -in:sent`
  - ecbf.info 宛に自動転送された通知が tosinariy の受信箱に入るため、`to:ecbf.info` で拾える。
- 補助（送信元指定）：
  - 京都信用金庫：`from:kyoto-shinkin.jp`（例 ksb-biz@kyoto-shinkin.jp／件名「入出金明細のご連絡」）
  - 三井住友銀行：`from:smbc.co.jp`（本物の通知。bizocean/アメーバ等の広告メルマガは対象外）
  - Square：`from:squareup.com OR from:messaging.squareup.com`（売上・入金通知）
- 手動転送（Fwd:）で届いた分：`from:ecbf.info@gmail.com subject:Fwd` も確認。

## 自動チェックの手順（毎日・ゆきが実行）
1. 上記クエリで LAST_CHECK 以降の新規通知メールを取得（`search_threads` → 必要に応じ `get_thread` で全文）。
2. 各メールから 日付・種別（入金/出金）・金額・金融機関/サービス・摘要・連絡番号等 を抽出。
   - **口座番号・残高は記録しない**（メール上マスク済みの口座番号もそのまま扱い、残高は転記しない）。
3. `入出金・売上帳簿_2026.xlsx`（同一様式）へ追記・再生成、`.md` 要約を更新。
4. LAST_CHECK を実行時刻に更新し、リポジトリへコミット/プッシュ。
5. Drive の売上帳簿フォルダへ、更新版を CSV→Google スプレッドシート（日付入りタイトル）で保存。
6. **入金（売上）または出金があった時のみ**、会話＋PushNotification で要点（件数・金額・要確認）を報告。無ければ LAST_CHECK だけ更新して静かに終了。

## 注意
- 広告メルマガ（bizocean/アメーバ/tsite 等）は「三井住友」「Square」等の語を含んでも**対象外**。送信元ドメインで銀行・決済サービス本体か判別する。
- 相手先名が通知に無い場合（京信等）は摘要を「入金 N件／出金 N件」とし、詳細は各サービスの明細参照とする。
